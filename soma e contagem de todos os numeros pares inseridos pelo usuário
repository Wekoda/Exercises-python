soma = 0
cont = 0
#for
for c in range(1 , 7):
    n1 = int(input('Digite o {} valor: '.format(c)))
    if n1 % 2 == 0:
        soma += n1
        cont = cont + 1
print('A soma dos valores pares é {} dos {} numeros pares informados'.format(soma, cont))
.
